# Wiener Linien Fleet Spotter - Dependencies
# Requires Python 3.10-3.12 (developed/tested on 3.11).
# Python 3.13+ has no matching wheel for torch<2.6 and will fail to install.
# Core ML
ultralytics==8.2.18
# <2.6 pin: torch 2.6 flipped torch.load's weights_only default to True,
# which breaks ultralytics==8.2.18's checkpoint loading (UnpicklingError)
torch>=2.0.0,<2.6
torchvision>=0.15.0,<0.21

# Web UI
streamlit==1.35.0
Pillow>=10.0.0

# Data / Augmentation
# <2 pin: tested with numpy 1.26; numpy 2.x can break ultralytics==8.2.18.
numpy>=1.24.0,<2
opencv-python-headless>=4.8.0
albumentations>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Annotation / Dataset utilities
roboflow>=1.1.0
PyYAML>=6.0

# Notebook support
jupyter>=1.0.0
ipywidgets>=8.0.0

# Utils
tqdm>=4.65.0
pandas>=2.0.0
